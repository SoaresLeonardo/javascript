function antcs() {
    var numero =  prompt('Digite um número inteiro qualquer')
    var x = numero -1
    var y = x +2
    window.alert(`Antes do ${numero}, Temos o número ${x}.
Depois de ${numero}, temos ${y}.`)
}